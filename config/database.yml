default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: transcription_app_development

test:
  <<: *default
  database: transcription_app_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
